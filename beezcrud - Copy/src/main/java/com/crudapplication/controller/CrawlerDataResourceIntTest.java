package com.crudapplication.controller;
/*
import javax.annotation.PostConstruct;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.web.querydsl.QuerydslPredicateArgumentResolver;

import com.crudapplication.repository.CrawlerDataRepository;

public class CrawlerDataResourceIntTest {
    
    @Autowired
    private QuerydslPredicateArgumentResolver querydslPredicateArgumentResolver;
    private Object MockMvcBuilders;
    private Object pageableArgumentResolver;
    private Object jacksonMessageConverter;         
    private Object MockitoAnnotations;

    @PostConstruct
    public void setup() {
        MockitoAnnotations.initMocks(this);
        CrawlerDataResourceIntTest crawlerDataResource = new CrawlerDataResourceIntTest();
        (MockMvcBuilders).standaloneSetup(crawlerDataResource)
            .setCustomArgumentResolvers(pageableArgumentResolver, querydslPredicateArgumentResolver)
            .setMessageConverters(jacksonMessageConverter).build();
    }
    
}
*/